const divide = (a,b) => {
    console.log(`divide ${a}`);
    console.log(`divide ${b}`);
    return a / b;
}
divide(2000,100);

function square(x){
    console.log(`multiply ${x}`);
    return Math.abs(x ** 2);
}
square(2);

const add = (c,d) => {
    console.log(`add ${c} ${d}`);
    return c + d;
}
add(3,4)